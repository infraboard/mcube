syntax = "proto3";

package infraboard.mcube.resource;
option go_package = "github.com/infraboard/mcube/pb/resource";

enum VISIABLE {
    // 默认Namespace可见
    NAMESPACE = 0;
    // 域内可见
    DOMAIN = 1;
    // 全局可见
    GLOBAL = 2;
}