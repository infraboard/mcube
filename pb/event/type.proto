syntax = "proto3";

package mcube.event;
option go_package = "github.com/infraboard/mcube/bus/event";

enum Type {
    // 全局配置, 所有服务可以读取
    OperateEvent = 0;
    // 组内配置, 组里面的服务可以读取
	StatusEvent = 1;
}